---
role: "mongshell-menubar 의 프로덕트 가치·범위·설계 원칙의 단일 권위 — 새 기능 제안의 정합성 판단 출발점. 구현 방법이나 기술 선택 근거는 다루지 않음."
kind: conceptual
non_goals:
  - "기술 스택·데이터 흐름 사양 (docs/architecture.md)"
  - "개별 기술 선택의 근거와 결과 (docs/architecture-decisions.md)"
  - "코드 작성 기준 (docs/code-standards.md)"
---

# PHILOSOPHY

## Core Value Proposition

**Claude 를 쓰는 도중에 한도를 확인하러 가지 않아도 되게 한다.**

Claude 구독 사용량은 원래 CLI 를 열어 `/usage` 를 쳐야 보인다. 작업 흐름을 끊는 그 왕복이 문제의 전부다. 이 앱은 5시간·7일 한도를 메뉴바에 상시 띄워, **아무것도 하지 않아도 눈에 들어오게** 만든다. 자세히 보고 싶을 때만 클릭한다.

따라서 이 프로덕트의 성공 조건은 기능 수가 아니라 **글랜스(glance) 품질**이다. 한 번 흘긋 봐서 "지금 여유가 있는가" 를 판단할 수 없다면 실패다.

## In Scope

- **메뉴바 상시 표시** — 5시간·7일 사용률, 그리고 놓치기 쉬운 5시간 창의 초기화 시각.
- **클릭 시 상세 팝오버** — 정확한 %, 두 한도의 초기화 시각, 모델별 주간 사용량, 계정 상태.
- **임계 상황 알림** — 색상 3단계 + 위험 시 맥동 + 시스템 알림.
- **로컬 개발 환경의 상태 통합** — 이미 이 맥에 있는 것 중 "글랜스로 알면 좋은 것" 은 같은 아이템에 붙일 수 있다 (예: openclaw 게이트웨이 건강 상태).
- **Claude Code 설정의 GUI 편집** — CLI 를 열지 않고 자주 바꾸는 몇 개 키만.
- **혼자 쓰거나, 아는 사람에게 빌드해서 주는 배포.**

## Out of Scope

- **사용량 이력·통계·차트.** 지금 여유가 있는지가 질문이고, 어제 얼마를 썼는지는 아니다. 시계열 저장은 DB 를 부르고 앱의 성격을 바꾼다.
- **여러 계정 동시 표시.** 1인 1계정 전제. 계정 전환은 로그아웃/로그인으로 충분하다.
- **`settings.json` 의 전체 편집기.** `hooks`·`env`·MCP 같은 자유형 키는 GUI 로 안전하게 다룰 수 없다 — 5개 키만 다루고 나머지는 보존만 한다.
- **App Store 배포.** 비공식 엔드포인트와 Keychain 공유 접근이 심사·샌드박스와 양립하지 않는다.
- **공식 API 로의 전환 시도.** 구독 한도에는 공식 API 가 없다. 있는 것처럼 가장하지 않고, 비공식 의존이라는 사실을 README 에 명시한 채로 간다.
- **범용 시스템 모니터.** CPU·메모리·네트워크는 다른 앱의 일이다.

## Design Principles

1. **글랜스 우선, 클릭은 보조.** 메뉴바 폭은 유한하다. 상시 표시할 정보를 늘리는 제안은 기본적으로 거절이고, 늘리려면 무엇을 뺄지 함께 제시해야 한다.
2. **없으면 없는 대로 완전히 사라진다.** 선택 기능(openclaw, Claude Code 설정)은 그 전제가 이 맥에 없으면 메뉴바·팝오버·설정 어디에도 흔적을 남기지 않는다. "회색으로 비활성" 도 시각적 소음이다.
3. **사용자의 파일은 손님의 물건이다.** `~/.claude/settings.json` 은 CLI 와 공유하는 사용자 자산이다. 앱이 모르는 것은 보존하고, 확신이 없으면 쓰지 않는다.
4. **토큰은 이 맥을 떠나지 않는다.** 자격증명은 Keychain 에만 있고 Anthropic 호스트 외 어디에도 전송하지 않는다. 텔레메트리·크래시 리포터를 붙이지 않는다.
5. **의존성 0 을 유지한다.** Foundation·SwiftUI·AppKit 만으로 만든다. 외부 패키지 하나가 Xcode 없는 빌드와 재현성을 깨뜨린다.
6. **비공식성을 숨기지 않는다.** 엔드포인트가 언제든 막힐 수 있다는 전제로 만들고, 그 사실을 README 와 코드 주석에 남긴다.
